const months = ["january", "fabruary", "march", "april", "may"];
months.forEach((months) => {
  console.log(months);
});
months.forEach((months, index, array) => {
  console.log(months, index, array);
});
